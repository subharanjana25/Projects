// Spinner.js
import React from 'react';
import './Spinner.css'; // Include some styles for your spinner

const Spinner = () => {
    return (
        <div className="spinner">
            <div className="loader"></div>
        </div>
    );
};

export default Spinner;
